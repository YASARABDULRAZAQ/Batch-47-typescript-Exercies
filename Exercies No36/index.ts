/*T-Shirt: Write a function called make_shirt() that accepts a size and the text of a message that should be printed on the shirt. The function should print a sentence summarizing the size of the shirt and the message printed on it. Call the function.*/

//Defining s function that acccepts asize and the
//text of a message that should be printed on the shirt.
{
const make_shirt=(size: string, message:string): void=>

{
    console.Log(`size:${size},Message:'${message}'`;)
//call the function with the size and the message

make_shift("Medium","TypScript Insights with Yasar")

let size:string="medium";
let message :string="TypeScript Insights with Yasar"
}

make_shirt(size, message);

 //directly call the function

make_shir("Large","Typescri insights");

}